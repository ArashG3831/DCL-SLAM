<?xml version="1.0" encoding="UTF-8"?>
<!-- WSL simulation profile: keep DDS traffic on the WSL loopback. -->
<CycloneDDS xmlns="https://cdds.io/config">
  <Domain id="any">
    <General>
      <Interfaces>
        <NetworkInterface address="127.0.0.1" multicast="false"/>
      </Interfaces>
      <AllowMulticast>false</AllowMulticast>
      <EnableMulticastLoopback>true</EnableMulticastLoopback>
      <!-- The former 1025/1472-byte limits were intended as an MTU
           workaround, but CycloneDDS 0.10.x can overrun its send iovec for
           fragmented ROS samples at such low limits.  WSL simulation stays
           on the loopback interface, so use the supported defaults/large
           fragment bounds instead.  The Wi-Fi profile remains unchanged. -->
      <FragmentSize>4000B</FragmentSize>
      <MaxMessageSize>14720B</MaxMessageSize>
      <MaxRexmitMessageSize>14720B</MaxRexmitMessageSize>
    </General>
    <Discovery>
      <ParticipantIndex>auto</ParticipantIndex>
      <MaxAutoParticipantIndex>200</MaxAutoParticipantIndex>
    </Discovery>
  </Domain>
</CycloneDDS>
